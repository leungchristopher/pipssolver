# bayesian pips

Today's NYT hard puzzle. Min-sum belief propagation with exact backtracking and solution validation.

[Method](METHOD.md)

```sh
npm install
npm run dev
```

`npm test` runs the solver and API checks. `npm run build && npm start` serves production on port 3000.
